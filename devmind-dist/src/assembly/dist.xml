<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/ASF/2.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASF/2.0.0 https://maven.apache.org/xsd/assembly-2.0.0.xsd">
    <id>dist</id>
    <formats>
        <format>tar.gz</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>devmind-${project.version}</baseDirectory>

    <!-- libs/：devmind-app 瘦 jar + 全部运行时依赖散 jar（不打 fat jar，便于单 jar 替换更新）；
         runner shaded jar 单独落 runner/（bin/dev-mind-agent 直接 -jar 启动），不进 libs -->
    <dependencySets>
        <dependencySet>
            <outputDirectory>libs</outputDirectory>
            <useTransitiveDependencies>true</useTransitiveDependencies>
            <unpack>false</unpack>
            <scope>runtime</scope>
            <excludes>
                <exclude>com.devmind:devmind-agent-runner</exclude>
            </excludes>
        </dependencySet>
    </dependencySets>

    <files>
        <!-- runner/：CAP-34 服务端零执行，会话/问答由 runner 执行——分发包附带 agent runner -->
        <file>
            <source>../devmind-agent-runner/target/devmind-agent-runner.jar</source>
            <outputDirectory>runner</outputDirectory>
        </file>
    </files>

    <fileSets>
        <!-- ui/：前端构建产物（只外置，jar 内不含静态资源；需先 npm run build） -->
        <fileSet>
            <directory>../frontend/dist</directory>
            <outputDirectory>ui</outputDirectory>
        </fileSet>
        <!-- config/：精简外置配置（常用项，未列出的回落 jar 内默认值）+ 说明文档 -->
        <fileSet>
            <directory>src/main/dist/config</directory>
            <outputDirectory>config</outputDirectory>
        </fileSet>
        <!-- bin/：服务控制脚本 -->
        <fileSet>
            <directory>src/main/dist/bin</directory>
            <outputDirectory>bin</outputDirectory>
            <fileMode>0755</fileMode>
        </fileSet>
        <!-- data/ 占位 -->
        <fileSet>
            <directory>src/main/dist/data</directory>
            <outputDirectory>data</outputDirectory>
        </fileSet>
    </fileSets>
</assembly>
